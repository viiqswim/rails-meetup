<div class="container center">
  <div class="row">
    <div class="col-md-12">
      <h2>Group Members</h2>
      <div class="btn-group">
        <%= link_to "See Organizers", group_organizers_path, class: "btn btn-info" %>
      </div>
      
      <hr />
      
      <table class="table table-striped table-bordered">
        <thead class="thead-inverse">
          <tr>
            <td><b>First Name</b></td>
            <td><b>Last Name</b></td>
            <td><b>Role</b></td>
          </tr>
        </thead>
        <tbody>
          <% @users.each do |user| %>
            <tr>
              <td><%= user.first_name %></td>
              <td><%= user.last_name %></td>
              <td><%= user.role(@group.id).name %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>